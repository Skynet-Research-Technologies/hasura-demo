require('dotenv').config();

// Global test configuration
jest.setTimeout(10000);  // Set timeout to 10s

// Global test setup
beforeAll(() => {
    // Run before all tests
});

afterAll(() => {
    // Run after all tests
});